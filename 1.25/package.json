{
  "name": "streamshield-proxy",
  "version": "1.25",
  "description": "Optimized StreamShield Proxy for fast TV channel switching",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "start:dev": "NODE_ENV=development node --no-buffering index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "node-fetch": "^2.6.1",
    "lru-cache": "^6.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
